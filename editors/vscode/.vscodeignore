# Source / build inputs — out/ is the bundled artefact vsce ships.
src/**
tsconfig.json
esbuild.mjs

# Dev tooling.
node_modules/**
.gitignore

# Map files — useful in dev, dead weight in the published VSIX.
out/**/*.map
